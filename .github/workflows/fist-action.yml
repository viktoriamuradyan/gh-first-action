name: First Workflow
on: workflow_dispatch
jobs:
  first job:
    runs_on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
      - name: Print Bye
        run: echo "Done - Bye!"
